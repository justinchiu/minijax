{
  "name": "minijax-ts",
  "version": "0.1.0",
  "description": "MiniJax-TS: a minimal JAX-like interpreter in TypeScript",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "node --experimental-strip-types src/test.ts",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
